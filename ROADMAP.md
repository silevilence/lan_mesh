# 🗺️ Product Roadmap

## 📝 计划中 (Planned)

- [x] **任务15:Windows客户端 — 群组管理界面**
    - [x] 创建群组界面
        - [x] 实现网卡选择下拉与群组名称输入表单
        - [x] 实现提交后调用创建群组 command 并展示结果
    - [x] 加入群组界面
        - [x] 实现自动发现列表的展示与刷新
        - [x] 实现点击列表项发起加入的交互
        - [x] 实现手动输入IP/端口的表单与提交逻辑
    - [x] 成员列表展示
        - [x] 实现成员在线状态展示(在线/离线)
        - [x] 实现直连/多跳可达标记展示

- [x] **任务16:Windows客户端 — 聊天界面**
    - [x] 群聊界面
        - [x] 实现消息列表按时间顺序展示
        - [x] 实现本机/他人消息的样式区分
        - [x] 实现文本输入框与发送按钮交互
        - [x] 实现文件选择与发送交互
        - [x] 实现消息发送中/已送达状态标记展示
    - [x] 单聊界面
        - [x] 实现独立单聊会话窗口(与群聊分开维护消息列表)
        - [x] 实现从成员列表发起单聊的入口交互

- [ ] **任务17:Windows客户端 — 文件传输界面**
    - [ ] 发送进度展示
        - [ ] 实现发送进度百分比与速度展示
    - [ ] 接收进度展示
        - [ ] 实现接收进度展示
        - [ ] 实现传输失败后的重试按钮交互(调用断点续传接口)

- [ ] **任务18:Windows客户端 — 网络与角色配置界面**
    - [ ] 网卡配置
        - [ ] 实现列出本机可用网络接口的界面
        - [ ] 实现选择结果传递给创建群组/加入群组流程
    - [ ] 拓扑状态展示
        - [ ] 实现当前邻居连接列表的简易展示
            - 任务描述:用于排查连通性问题,不要求做复杂拓扑图可视化。

- [ ] **任务19:Android客户端 — 核心库绑定**
    - [ ] FFI/JNI绑定
        - [ ] 在 core-ffi crate 中实现暴露给 JNI 调用的接口(加入群组、发送消息、发送文件、获取成员列表)
            - 任务描述:仅暴露 Leaf 角色相关能力,不编译/暴露 Relay、多连接、路由发现相关逻辑。
        - [ ] 配置 Android 端 Rust 交叉编译产物集成
            - 任务描述:确保生成的动态库能被 Android 工程正确加载。
    - [ ] Kotlin封装层
        - [ ] 实现 Kotlin 侧对 JNI 接口的方法封装
        - [ ] 实现异步回调/事件桥接到 Kotlin 协程或回调接口

- [ ] **任务20:Android客户端 — 前台服务与后台保活**
    - [ ] 前台服务搭建
        - [ ] 实现承载长连接与消息收发的前台服务
        - [ ] 实现持续通知栏提示的展示与更新
    - [ ] 电量与系统适配
        - [ ] 实现引导用户加入电池优化白名单的提示流程
    - [ ] 多群组调度
        - [ ] 实现单一前台服务内管理多个群组会话实例的调度逻辑
            - 任务描述:避免每个群组单独起服务导致资源浪费。

- [ ] **任务21:Android客户端 — 群组管理界面**
    - [ ] 加入群组界面
        - [ ] 实现自动发现列表展示与刷新
        - [ ] 实现手动输入IP/端口加入的表单
            - 任务描述:不提供创建群组入口。
    - [ ] 成员列表展示
        - [ ] 实现成员在线状态展示,适配移动端交互习惯

- [ ] **任务22:Android客户端 — 聊天界面**
    - [ ] 群聊界面
        - [ ] 实现消息展示列表(适配气泡样式与滚动)
        - [ ] 实现文本输入框交互
        - [ ] 实现调用系统文件选择器发送文件的交互
    - [ ] 单聊界面
        - [ ] 实现单聊会话入口与独立消息列表展示

- [ ] **任务23:Android客户端 — 文件传输与存储适配**
    - [ ] 传输进度界面
        - [ ] 实现发送/接收进度条与状态提示展示
    - [ ] 分区存储适配
        - [ ] 实现文件读写权限申请流程
        - [ ] 实现基于分区存储机制的文件访问路径适配

- [ ] **任务24:Android客户端 — 权限与网络适配**
    - [ ] 权限与多播锁
        - [ ] 实现网络相关权限的申请流程
        - [ ] 实现设备发现所需 MulticastLock 的获取与释放,兼容不同Android版本差异
    - [ ] 网络状态监听
        - [ ] 实现监听Wi-Fi切换/断开等网络状态变化
        - [ ] 实现网络变化时触发核心库重连逻辑并向用户提示当前连接状态

## 🚧 开发中 (In Progress)

## ✅ 已完成 (Completed)

- [x] **任务00:项目结构初始化**
    - [x] 使用 cargo new --lib core 创建核心通信库 crate
        - 任务描述:核心逻辑作为独立 lib crate,供 Tauri 与未来 Android 端(通过 FFI/JNI)共同依赖,不直接包含任何 UI 相关代码。
    - [x] 在根目录创建顶层 Cargo.toml 并声明 workspace members
        - 任务描述:根 Cargo.toml 仅作为 workspace 容器,members 字段包含 core、后续的 tauri 壳工程等子 crate,统一管理依赖版本。
    - [x] 使用 cargo new --lib core-ffi 预留 FFI 适配层(暂不实现内容)
        - 任务描述:预留一个供 Android JNI 绑定使用的 crate 位置,当前只建空壳,不写实现。
    - [x] 执行 cargo add tokio --features full 为 core crate 引入异步运行时
        - 任务描述:核心库网络/并发相关代码统一基于 tokio 异步模型开发。
    - [x] 执行 cargo add serde serde_json 为 core crate 引入序列化依赖
        - 任务描述:确认序列化选型为 JSON,serde 与 serde_json 组合作为消息编解码基础。
    - [x] 执行 cargo add uuid --features v4 为 core crate 引入唯一ID生成依赖
        - 任务描述:用于消息唯一ID、设备ID、群组ID等标识生成。
    - [x] 执行 cargo build 验证初始工程可编译通过
        - 任务描述:确认 workspace 结构、依赖声明无误,作为后续所有开发任务的起点基线。
    - [x] 生成 .gitignore 并补充 Rust/Tauri/Android 常见忽略项
        - 任务描述:确保 target/、node_modules/、Android build/ 等产物目录不进入版本库。
    - [x] 建立 README.md 记录 workspace 结构说明
        - 任务描述:简要说明各 crate 用途(core、core-ffi、tauri 壳、Android 工程),便于后续任务承接者快速理解工程布局。

- [x] **任务01:基础数据结构定义**
    - [x] 设备与群组标识类型
        - [x] 定义 DeviceId 类型(基于 UUID 封装)
            - 任务描述:封装 UUID,避免各模块直接裸用字符串表示设备身份。
        - [x] 定义 GroupId 类型(基于 UUID 封装)
            - 任务描述:封装 UUID,用于唯一标识群组会话。
        - [x] 定义设备角色枚举(Relay / Leaf)
            - 任务描述:Windows 端可为 Relay 或 Leaf,Android 端固定为 Leaf,供后续逻辑分支判断使用。
    - [x] 消息公共头部结构
        - [x] 定义消息头字段:消息ID、群组ID、来源设备ID
            - 任务描述:作为所有消息类型共享的前置字段,便于统一做去重判断与来源追溯。
        - [x] 定义消息头字段:目标标识(群发/指定设备ID)
            - 任务描述:用于区分群发消息与单聊消息,供路由模块判断转发方式。
        - [x] 定义消息头字段:TTL/跳数、时间戳
            - 任务描述:TTL 用于防止路由发现类消息无限扩散,时间戳用于消息排序与去重缓存过期判断。
    - [x] 消息类型与payload结构
        - [x] 定义文本消息 payload 结构
            - 任务描述:包含文本内容字段,结构尽量简单。
        - [x] 定义文件分片消息 payload 结构
            - 任务描述:包含文件ID、分片序号、分片总数、分片二进制数据字段。
        - [x] 定义心跳消息 payload 结构
            - 任务描述:轻量结构,仅需设备ID与时间戳,用于连接保活判断。
        - [x] 定义成员变更通知 payload 结构
            - 任务描述:包含变更设备ID与变更类型(上线/离线)。
        - [x] 定义路由发现请求/应答 payload 结构
            - 任务描述:请求结构包含目标设备ID与已经过路径记录;应答结构包含确认路径信息。
        - [x] 使用 serde 内部标签机制统一消息类型枚举
            - 任务描述:通过 serde 的 tag 机制在 JSON 中区分消息类型,反序列化时能自动匹配到对应 payload 结构。

- [x] **任务02:JSON 序列化与反序列化**
    - [x] 消息结构体与 JSON 互转
        - [x] 实现消息结构到 JSON 字符串的序列化函数
            - 任务描述:基于 serde_json 完成转换,统一字段命名风格(如 snake_case)。
        - [x] 实现 JSON 字符串到消息结构的反序列化函数
            - 任务描述:处理反序列化失败场景(格式错误、字段缺失)的错误返回。
    - [x] 文件二进制内容编码方案
        - [x] 评估 Base64 内嵌 JSON 方案与 JSON头+二进制体混合帧方案
            - 任务描述:对比两种方案在传输体积膨胀率与解析复杂度上的差异,给出结论。
            - 结论:选用 Base64 内嵌 JSON,体积约增加 33%,但保持单一 JSON 帧解析路径。
        - [x] 实现选定方案的编码函数
            - 任务描述:将文件分片二进制数据按选定方案转换为可通过 JSON 消息传输的形式。
        - [x] 实现选定方案的解码函数
            - 任务描述:从接收到的消息中还原出原始二进制分片数据。
    - [x] TCP 消息帧边界处理
        - [x] 实现带长度前缀的写入编码(发送端)
            - 任务描述:发送前在消息体前附加固定字节数的长度字段,标明消息体长度。
        - [x] 实现带长度前缀的读取解码(接收端)
            - 任务描述:接收时先读取长度字段,再读取对应长度的消息体,解决TCP粘包/拆包问题。

- [x] **任务03:网络连接管理 — 监听与连接建立**
    - [x] TCP 监听端(Listener)实现
        - [x] 实现绑定指定端口开始监听的逻辑
            - 任务描述:仅 Relay 角色调用,支持接受多个入站连接。
        - [x] 实现接受新连接并注册为邻居连接的逻辑
            - 任务描述:每个新入站连接需要加入本地邻居连接集合,并分配读写任务。
    - [x] TCP 连接发起端(Connector)实现
        - [x] 实现主动连接指定IP和端口的逻辑
            - 任务描述:所有角色均可调用;Leaf 角色发起后即视为唯一邻居连接。
        - [x] 实现连接成功后加入邻居连接集合的逻辑
            - 任务描述:与监听端保持一致的连接注册方式,便于上层统一处理。
    - [x] 多网卡绑定支持(仅 Windows/Relay)
        - [x] 实现监听时绑定指定本地网卡IP的逻辑
            - 任务描述:避免系统默认路由选错网卡,需要显式指定监听地址。
        - [x] 实现发起连接时绑定指定本地网卡IP的逻辑
            - 任务描述:同上,确保连接从预期的网络接口发出。

- [x] **任务04:网络连接管理 — 心跳与断线处理**
    - [x] 心跳发送
        - [x] 实现定时心跳消息发送任务
            - 任务描述:为每条邻居连接启动独立的定时发送任务,间隔可配置。
    - [x] 心跳超时判定
        - [x] 实现接收心跳并刷新最近活跃时间记录
            - 任务描述:每条连接维护一个最近收到心跳的时间戳。
        - [x] 实现超时检测任务并触发断线处理
            - 任务描述:定期检查各连接最近活跃时间,超过阈值判定为离线。
    - [x] 断线清理与重连
        - [x] 实现连接断开后的资源清理逻辑
            - 任务描述:移除失效连接、更新邻居列表、触发成员离线通知。
        - [x] 实现 Leaf 角色断线后的指数退避重连逻辑
            - 任务描述:仅 Leaf 角色需要在唯一连接断开后自动重连;Relay 角色对断开的入站连接只清理不重连。

- [x] **任务05:群组会话管理 — 会话生命周期**
    - [x] 会话实例结构设计
        - [x] 定义会话实例的数据结构(连接集合、去重缓存、成员表)
            - 任务描述:每个群组对应一个独立会话实例,内部状态互相隔离。
    - [x] 会话创建与销毁
        - [x] 实现创建群组会话的接口(Relay 角色专用)
            - 任务描述:创建的同时启动监听逻辑(依赖任务03)。
        - [x] 实现加入群组会话的接口(建立到目标Relay的连接)
            - 任务描述:内部调用连接发起逻辑(依赖任务03)。
        - [x] 实现销毁会话的接口
            - 任务描述:释放该会话下所有连接与缓存资源。
    - [x] 多会话并存验证
        - [x] 编写测试验证单设备同时运行多个会话且状态互不干扰
            - 任务描述:验证例如设备同时在群组1为Relay、群组2为Leaf的场景下功能均正常。

- [x] **任务06:群组会话管理 — 成员发现与状态维护**
    - [x] 局域网自动发现
        - [x] 实现 Relay 角色的 UDP 广播/组播定时公告逻辑
            - 任务描述:周期性广播自身存在信息(设备ID、群组名等)。
        - [x] 实现 Leaf 角色的 UDP 广播监听逻辑
            - 任务描述:监听并收集局域网内可加入的 Relay 列表,供上层展示。
    - [x] 手动加入兜底方案
        - [x] 实现根据用户输入IP和端口直接加入群组的接口
            - 任务描述:跳过自动发现流程,直接调用连接发起逻辑。
    - [x] 成员状态同步
        - [x] 实现成员上线通知消息的构造与广播
            - 任务描述:新成员加入后向可达范围内广播上线通知。
        - [x] 实现成员离线通知消息的构造与广播
            - 任务描述:检测到成员离线(依赖任务04的断线判定)后广播离线通知。
        - [x] 实现本地成员列表根据通知消息的更新逻辑
            - 任务描述:各节点收到上下线通知后更新本地维护的成员视图。

- [x] **任务07:消息路由 — 去重与防环**
    - [x] 去重缓存结构
        - [x] 实现已处理消息ID的缓存数据结构(如带过期时间的HashSet)
            - 任务描述:记录近期已处理过的消息ID,供转发前查重使用。
    - [x] 缓存过期清理
        - [x] 实现定期清理超时消息ID记录的任务
            - 任务描述:避免缓存随时间无限增长,清理周期与消息ID有效期需权衡设定。
    - [x] 查重接口
        - [x] 实现"消息是否已处理过"的判断接口供转发模块调用
            - 任务描述:该接口在任务08、任务09的转发逻辑中作为前置判断使用。

- [x] **任务08:消息路由 — 群发洪泛转发**
    - [x] Relay 角色转发逻辑
        - [x] 实现收到群发消息后转发给除来源外所有邻居的逻辑
            - 任务描述:依赖任务07的去重判断,避免重复转发同一消息。
    - [x] Leaf 角色转发逻辑
        - [x] 实现收到非本机消息后原样转发给唯一邻居的逻辑
            - 任务描述:逻辑简单,不需要多路判断,仅需判断消息来源是否为该唯一邻居以避免原路发回。
    - [x] 本机消息注入
        - [x] 实现本机发送群聊消息时的初始注入与向邻居广播逻辑
            - 任务描述:本机作为消息发起点,需要先写入去重缓存再发送,避免消息环回时被自己重复处理。

- [x] **任务09:消息路由 — 单聊路径发现与点对点转发**
    - [x] 路径发现请求
        - [x] 实现发起路径发现请求消息的构造与广播逻辑
            - 任务描述:附带跳数限制(TTL),防止无限扩散。
        - [x] 实现中间节点转发请求并记录反向来源的逻辑
            - 任务描述:沿途节点需要临时记录"该请求从哪个邻居方向来",用于后续应答原路返回。
    - [x] 路径确认应答
        - [x] 实现目标设备收到请求后构造应答消息的逻辑
            - 任务描述:应答沿反向路径逐跳返回。
        - [x] 实现发起方收到应答后记录可用路径的逻辑
            - 任务描述:记录的路径信息供后续点对点消息使用,避免每次都重新洪泛发现。
    - [x] 点对点转发
        - [x] 实现已知路径下单聊消息的定向转发逻辑
            - 任务描述:沿已记录路径逐跳转发,不做全邻居广播。
    - [x] 路径失效处理
        - [x] 实现检测路径上连接断开并清除对应路径记录的逻辑
            - 任务描述:依赖任务04的断线检测结果。
        - [x] 实现路径失效后重新触发路径发现的逻辑
            - 任务描述:下次该目标有消息发送需求时自动重新走一次路径发现流程。

- [x] **任务10:消息路由 — 拓扑动态更新**
    - [x] 设备离线清理
        - [x] 实现设备离线后清理其相关路径缓存的逻辑
            - 任务描述:依赖任务06的离线通知与任务09的路径记录结构。
        - [x] 实现设备离线后清理其相关去重记录关联信息的逻辑
            - 任务描述:避免残留的去重记录影响后续同ID消息判断(视具体缓存结构是否需要此步骤)。

- [x] **任务11:文件传输 — 分片与流式转发**
    - [x] 分片规则
        - [x] 定义固定分片大小常量
            - 任务描述:约定统一分片大小,发送前计算分片总数。
        - [x] 实现文件读取并按分片大小切分的逻辑
            - 任务描述:发送端将本地文件切分为多个分片消息。
    - [x] 接收端组装
        - [x] 实现接收分片并按序号写入临时文件的逻辑
            - 任务描述:接收端需要处理分片可能不按顺序到达的情况。
    - [x] Relay 流式转发
        - [x] 实现 Relay 角色对文件分片的内存直转发逻辑(不整文件落盘)
            - 任务描述:边收边发,避免大文件占用过多中继设备内存/磁盘。

- [x] **任务12:文件传输 — 断点续传与完整性校验**
    - [x] 接收进度记录
        - [x] 实现记录已成功接收分片序号集合的逻辑
            - 任务描述:供中断后续传时判断哪些分片还需要请求。
    - [x] 续传请求
        - [x] 实现接收端请求剩余分片的消息构造与发送逻辑
            - 任务描述:传输中断恢复连接后,接收端主动请求缺失分片列表。
        - [x] 实现发送端响应续传请求重新发送指定分片的逻辑
            - 任务描述:仅重发缺失部分,不重传已确认接收的分片。
    - [x] 完整性校验
        - [x] 实现全部分片接收完成后的哈希计算逻辑(如SHA256)
            - 任务描述:发送端在文件头信息中附带原始文件哈希值。
        - [x] 实现校验失败后提示重新传输的逻辑
            - 任务描述:校验不通过时向上层返回失败状态,由调用方决定是否重新发起传输。

- [x] **任务13:单聊功能支持**
    - [x] 消息目标区分
        - [x] 实现根据消息头目标标识判断群发/单聊的分支逻辑
            - 任务描述:目标为群发标记时走任务08洪泛逻辑,目标为指定设备ID时走任务09点对点逻辑。
    - [x] 接口封装
        - [x] 实现供上层调用的"发送群聊消息"接口
            - 任务描述:封装消息头填充与调用洪泛转发的过程。
        - [x] 实现供上层调用的"发送单聊消息"接口
            - 任务描述:封装消息头填充与调用路径发现/点对点转发的过程。

- [x] **任务14:Windows客户端 — Tauri项目搭建**
    - [x] 项目初始化
        - [x] 创建 Tauri 子工程并加入 workspace
            - 任务描述:在 workspace 下新增 Tauri 子工程目录。
        - [x] 通过本地路径依赖引用 core crate
            - 任务描述:Tauri 后端 Rust 代码直接调用 core crate 提供的接口,不重复实现网络逻辑。
    - [x] Tauri command 接口定义
        - [x] 定义创建群组 command
        - [x] 定义加入群组(自动发现/手动IP)command
        - [x] 定义发送群聊/单聊文本消息 command
        - [x] 定义发送文件 command
        - [x] 定义获取成员列表 command
        - [x] 定义获取连接/邻居状态 command
            - 任务描述:以上每个 command 仅做参数接收与调用 core crate 对应接口的转接,不实现业务逻辑。
    - [x] 前后端事件通信
        - [x] 实现核心库"收到新消息"事件到前端的推送
        - [x] 实现核心库"成员上下线"事件到前端的推送
        - [x] 实现核心库"传输进度更新"事件到前端的推送
            - 任务描述:均使用 Tauri 事件系统主动推送,不采用前端轮询方式。
