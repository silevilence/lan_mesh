# CHANGELOG

## V0.4.1

### 🚀 优化

- **成员昵称实时同步**：群组成员修改昵称后，成员列表即时更新显示，无需重新拉取即可看到最新昵称。
- **升级安装保留快捷方式**：更新安装时保留你对桌面与开始菜单快捷方式的选择，升级完成后自动重建快捷方式并刷新图标缓存，不再残留旧版图标显示。

### 🐛 Bug 修复

- **修复重新加入群组后身份变化**：重新加入已保存的群组时保持原有设备身份，不再在成员列表中显示为新设备。
- **修复单聊中误关整个会话**：单聊页面的离开按钮改为"返回群聊"，点击后回到群聊界面，不再关闭整个会话。

## V0.4.0

### ✨ 新功能

- **系统托盘驻留**：关闭或最小化窗口时程序驻留到系统托盘继续运行，可通过托盘菜单打开主界面或退出程序；首次驻留时有提示说明，也可在配置页关闭"关闭时最小化到托盘"。
- **消息桌面通知**：窗口隐藏或最小化期间收到新消息时弹出 Windows 桌面通知，点击通知可直接打开主界面；可在配置页开关通知。
- **更新互助**：检查到新版本并下载后，可把保留的安装包分享给局域网群组中的其他设备；其他设备收到更新包并完成完整性校验后，可一键确认安装，程序会自动退出并启动本地安装程序。
- **成员昵称广播**：设置的昵称会广播给群组其他成员，对方无需手动添加即可看到你的昵称。
- **多网卡并行中继发现**：加入群组时的中继发现改为在所有本机网卡上并行扫描，多网卡环境下发现更快速、更可靠。

### 🚀 优化

- **界面细节**：窗口头部改用应用图标，配置页开关项布局更整齐。

## V0.3.0

### ✨ 新功能

- **群组自动保存与恢复**：加入过的群组会自动记录在本地，下次启动客户端时按角色与可达性自动还原连接，无需重新输入信息重新加入。
- **断连群组后台自动重连**：暂时连不上的群组会在后台周期性重试，恢复后自动重新加入，无需手动操作。
- **群组成员列表**：聊天界面新增成员列表展示，成员上线/下线状态实时刷新。
- **聊天区粘贴与拖拽发送文件**：可直接在聊天区粘贴图片或多文件发送，也可将文件拖入聊天区发送。
- **配置界面**：新增一级配置页，收纳检查更新等设置项。
- **多行文本发送快捷键可配置**：可在配置中选择 Enter 或 Ctrl+Enter 发送消息，Shift+Enter 始终换行。
- **昵称设置**：可在配置中设置昵称，群聊与单聊中以"昵称(ID)"形式展示。
- **非群主查看连接信息与分享邀请码**：非群主也能查看群组连接信息（指向群主）并生成分享邀请码。

### 🚀 优化

- **按钮交互反馈**：按钮增加悬停与按下动态效果，选择文件按钮缩小更紧凑。
- **版本号显示**：标题栏与配置界面展示当前客户端版本号。

## v0.2.1

### 🚀 优化

- **客户端运行更轻量稳定**：网卡列表读取与文件选择不再借助系统 PowerShell 完成，改为原生实现，运行时不再启动外部子进程，降低了资源占用，也避免了部分环境下安全软件对外部子进程的误报与拦截。

### 🐛 Bug 修复

- **修复长文本消息溢出气泡的问题**：发送长串文字或长链接时不再撑破消息气泡，会自动按宽度换行；同时保留了你输入时的换行与空格格式。

## v0.2.0

### ✨ 新功能

- **一键分享码快速加群**：群组创建者可在分享对话框生成分享码，其中包含群组名称、群组 ID 与所有可连接的中继地址；新设备粘贴分享码即可自动填入群组信息，并自动探测可用的中继地址与本机网卡，无需手动逐项填写 IP 与端口。
- **客户端自动更新**：Windows 客户端接入在线更新能力，启动时自动检查新版本，也可手动触发检查；发现新版本后可查看版本号、发布时间与更新说明，确认后自动下载并安装，无需手动下载安装包。

### 🚀 优化

- **发布版隐藏控制台窗口**：正式发布版本运行时不再弹出黑色控制台窗口，仅显示主程序界面，视觉更清爽。

## v0.1.0

### ✨ 新功能

- **局域网多跳 Mesh 通信**：支持同一群组内任意数量设备加入，消息可通过中间设备多跳转发到达目标，无需全网互相连通，无需互联网与中心服务器。
- **群组创建与加入**：Windows 端可创建群组（作为中继）或加入已有群组（作为叶子），支持局域网自动发现可用群组，也可手动输入 IP 与端口加入。
- **群聊消息**：群发消息通过多跳洪泛到达群组内所有成员。
- **单聊消息**：通过路径发现建立定向转发路径，仅沿该路径传递消息，不做全群广播。
- **文件传输**：大文件自动分片发送，支持断点续传（中断后只补传缺失分片），接收完成后自动校验文件完整性；发送时携带文件名，接收方可直接保存。
- **成员状态管理**：实时展示群组成员上线/下线状态、直连与多跳可达标记。
- **连接保活**：邻居间定时心跳检测，超时自动判定离线；断线后自动重连。
- **多网卡选择**：创建或加入群组时可指定本机网卡，避免系统默认路由选错网络接口。
- **Windows 桌面客户端**：基于 Tauri 的原生客户端，提供群组管理、消息收发、文件传输、网络配置等完整界面。
- **文件接收与另存**：接收文件后可选择保存位置，支持多网卡环境下广播发现。
- **中文网卡名称兼容**：修复了 Windows 系统中网络接口名称含中文时的显示问题。
- **安装包分发**：支持打包为 NSIS 单文件安装程序，通过 LTO 与符号裁剪优化体积。
